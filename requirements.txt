# Core backend + agent (all platforms)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
watchdog>=6.0.0
xgrammar>=0.1.0
transformers>=4.45.0
ollama>=0.4.0
llama-cpp-python>=0.3.0

# Triton: official PyPI wheels are Linux-only. On Windows/macOS, pip install skips this line.
# For GPU kernel code in project/main.py, use WSL2/Linux or follow PyTorch docs for your OS.
triton>=3.0.0; sys_platform == "linux"
