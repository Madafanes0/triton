# Core backend + agent (all platforms)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
watchdog>=6.0.0
xgrammar>=0.1.0
transformers>=4.45.0
ollama>=0.4.0

# llama-cpp-python is NOT listed here: on Windows it often needs MSVC + CMake to build.
# Only install if you use Settings → llama.cpp (GGUF). Default agent backend is Ollama.
#   pip install -r requirements-llamacpp.txt

# Triton: official PyPI wheels are Linux-only.
triton>=3.0.0; sys_platform == "linux"
